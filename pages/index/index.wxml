<map show-location="{{ showLocation }}"
     scale="{{ scale }}"
     longitude="{{ longitude }}"
     latitude="{{ latitude }}">
  <cover-view  class="weather" bindtap="clickWeather"> Weather</cover-view>
</map>